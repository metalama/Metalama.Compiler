namespace Issue199CoreTargetsPath;

/// <summary>
/// The scenario fails during MSBuild evaluation of the CommunityToolkit.Mvvm targets, before any
/// compilation happens, so the content of this file is immaterial. It exists only so that the
/// project has something to compile once the build gets that far.
/// </summary>
internal sealed class Class1
{
    public int Value { get; init; }
}
